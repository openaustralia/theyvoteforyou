---
version: "3.9"

services:
  mysql:
    image: "mysql:latest"
    ports:
      - 3306:3306
    restart: "always"
    logging:
      driver: "journald"
    # volumes:
    #   - "/opt/data/mysql:/var/lib/mysql"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: "theyvoteforyou_development"
      MYSQL_USER: "theyvoteforyou"
      MYSQL_PASSWORD: "theyvoteforyou"

  # adminer:
  #   image: "adminer:latest"
  #   ports:
  #     - "8888:8080"
  #   restart: "always"
  #   logging:
  #     driver: "journald"
  #   environment:
  #     ADMINER_DEFAULT_SERVER: "mysql"
  #   depends_on:
  #     - mysql

  elasticsearch:
    image: "elasticsearch:6.8.23"
    ports:
      - "9200:9200"
      - "9300:9300"
    restart: "always"
    logging:
      driver: "journald"
    environment:
      "discovery.type": "single-node"
      "http.port": "9200"
      "http.cors.enabled": "true"
      "http.cors.allow-origin": "http://localhost:1358,http://127.0.0.1:1358"
      "http.cors.allow-headers": "X-Requested-With,X-Auth-Token,Content-Type,Content-Length,Authorization"
      "http.cors.allow-credentials": "true"
      "bootstrap.memory_lock": "true"
      "ES_JAVA_OPTS": "-Xms512m -Xmx512m"

  dejavu:
    image: "appbaseio/dejavu:latest"
    ports:
      - "1358:1358"
    restart: "always"
    logging:
      driver: "journald"
    depends_on:
      - elasticsearch

  mailpit:
    image: "axllent/mailpit:latest"
    ports:
      - "1025:1025" # SMTP
      - "1088:8025" # web UI
    restart: "always"
    logging:
      driver: "journald"

  # Runs the app itself in Docker, as an alternative to `bundle exec rails s` on
  # the host. Not used for production - see docs/adr/0003-rbenv-replaces-rvm.md.
  app:
    build:
      context: ../..
      args:
        RUBY_VERSION: ${RUBY_VERSION:-3.4.10}
    command: bash -c "bundle check || bundle install && bin/rails server -b 0.0.0.0"
    ports:
      - "3000:3000"
    volumes:
      - ../..:/app
      - bundle_cache:/usr/local/bundle
    environment:
      MYSQL_ROOT_PASSWORD: root
      ELASTICSEARCH_URL: "http://elasticsearch:9200"
      MAILPIT_HOST: mailpit
      RAILS_ENV: development
    depends_on:
      - mysql
      - elasticsearch
      - mailpit

volumes:
  bundle_cache:
