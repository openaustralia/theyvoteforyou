%p
  Sort by:
  = link_to_unless sort == "name", "Name", division_path2(division, {sort: "name", dmp: nil, display: params[:display]}, false)
  -# For senate hide this to match php app
  -# This isn't actually desired so change this
  - if division.australian_house == "representatives"
    |
    = link_to_unless sort == "constituency", "Electorate", division_path2(division, {sort: "constituency", dmp: nil, display: params[:display]}, false)
  |
  = link_to_unless sort.nil?, "Party", division_path2(division, {sort: nil, dmp: nil, display: params[:display]}, false)
  |
  = link_to_unless sort == "vote", "Vote", division_path2(division, {sort: "vote", dmp: nil, display: params[:display]}, false)
