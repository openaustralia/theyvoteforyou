%p
  Sort by:
  = link_to_unless sort == "name", "Name", division_path2_false(division, {sort: "name", dmp: nil, display: params[:display]})
  -# For senate hide this to match php app
  -# This isn't actually desired so change this
  - if division.australian_house == "representatives"
    |
    = link_to_unless sort == "constituency", "Electorate", division_path2_false(division, {sort: "constituency", dmp: nil, display: params[:display]})
  |
  = link_to_unless sort.nil?, "Party", division_path2_false(division, {sort: nil, dmp: nil, display: params[:display]})
  |
  = link_to_unless sort == "vote", "Vote", division_path2_false(division, {sort: "vote", dmp: nil, display: params[:display]})
