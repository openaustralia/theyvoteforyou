- content_for :title, "Policies"
- set_meta_tags description: "A list of policies available on #{Rails.configuration.project_name}. Policies are a group of votes which, taken together, indicate a particular stance on an issue. You can research and create your own."

- content_for :header do
  .section-header.clearfix
    .page-header.container
      %nav.header-actions.col-md-3
        - if policy(Policy).new?
          = link_to "New policy", new_policy_path, class: "link-policy-new btn btn-primary btn-xs"
      %h1= yield :title
      %p.lead
        Policies are a group of votes which, taken together, indicate a particular stance on an issue.

= render "policies_chooser", party: @party, house: @house, sort: @sort

- if @policies
  %ul.policies-list.list-unstyled
    = render @policies

- else
  %p
    There are no published policies yet. Go ahead and #{link_to "make the first one", new_policy_path}.

%p See also #{link_to "draft policies", drafts_policies_path}.
