- content_for :title do
  -# Friends of
  Друзі по голосуванню депутата/тки:
  = @member.full_name
-# set_meta_tags description: "#{members_type(@member.house)} that voted similarly to #{@member.name_without_title}."
- set_meta_tags description: "#{members_type(@member.house)}, який голосував/ла подібно до #{@member.name_without_title}."

= render "member_page_header", member: @member
= render "possible_friends", member: @member, all_friends: true
