%section.page-section#recent-votes
  %h2 Recent Votes

  %p
    Recent votes in parliament that this
    = member_type(member.house)
    could have attended.

  - member.person.members.order(entered_house: :desc).each do |member|
    = render "divisions/divisions2", members: [member], divisions: member.divisions_they_could_have_attended.limit(3), house: nil

  =link_to "View all votes", member_divisions_path_simple(member), class: 'btn btn-default btn-sm'
